number = 190
divisor = 17
remainder = number % divisor
if remainder == 0:
    result = number + divisor
else:
    result = number + (divisor - remainder)
print(f"минимальное число больше 190, делящееся на 17: {result}")
